apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent
  annotations:
    # Workload Identity: bind this KSA to the Google Service Account created by
    # Terraform. Replace PROJECT_ID (or use `terraform output
    # google_service_account_email`). Must match var.google_service_account_id
    # and the Terraform WI binding (namespace/ksa = agents/agent).
    iam.gke.io/gcp-service-account: agents-runtime@msteiner.iam.gserviceaccount.com
