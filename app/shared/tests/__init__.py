"""Unit tests for the shared agent library.

These live *inside* the ``shared`` package and use relative imports, so the
package stays self-contained: it can be dropped into another codebase at a
different import path and its tests keep resolving with no rewriting. Run them
here as ``app.shared.tests``.
"""
